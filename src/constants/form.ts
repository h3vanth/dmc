const UNREGISTER_OPTIONS = {
  keepDefaultValue: false,
  keepDirty: false,
  keepDirtyValues: false,
  keepError: false,
  keepIsValid: false,
  keepTouched: false,
  keepValue: false,
};

const RESET_OPTIONS = {
  keepDefaultValues: false,
  keepDirty: false,
  keepDirtyValues: false,
  keepErrors: false,
  keepIsSubmitted: false,
  keepIsValid: false,
  keepSubmitCount: false,
  keepTouched: false,
  keepValues: false,
};

export { UNREGISTER_OPTIONS, RESET_OPTIONS };
